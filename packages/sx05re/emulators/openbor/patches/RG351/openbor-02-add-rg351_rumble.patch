diff --git a/engine/Makefile b/engine/Makefile
index 24fdf485..482c5e0f 100644
--- a/engine/Makefile
+++ b/engine/Makefile
@@ -140,6 +140,7 @@ BUILD_PTHREAD   = 1
 BUILD_SDL_IO    = 1
 BUILD_VORBIS    = 1
 BUILD_WEBM      = 1
+BUILD_RG351_RUMBLE = 1
 BUILDING        = 1
 INCLUDES        = $(SDKPATH)/usr/include \
                   $(SDKPATH)/usr/include/SDL2
@@ -586,6 +587,11 @@ CFLAGS 	       += -DWEBM
 endif
 
 
+ifdef BUILD_RG351_RUMBLE
+CFLAGS 	       += -DRG351_RUMBLE
+endif
+
+
 CXXFLAGS        = $(CFLAGS) -fno-exceptions -fno-rtti
 ASFLAGS         = $(CFLAGS)
 
diff --git a/engine/sdl/control.c b/engine/sdl/control.c
index 9118fb1c..108b278b 100644
--- a/engine/sdl/control.c
+++ b/engine/sdl/control.c
@@ -860,6 +860,13 @@ void control_rumble(int deviceID, int ratio, int msec)
 {
     if (msec > 0 && devices[deviceID].haptic)
     {
+        #ifdef RG351_RUMBLE
+        if (strcmp(devices[deviceID].name, "OpenSimHardware OSH PB Controller") == false)
+        {
+            if (msec == 0) return;
+            msec *= 10;
+        }
+        #endif
         if (SDL_HapticRumblePlay(devices[deviceID].haptic, ratio, msec) != 0)
         {
             printf("Warning: Unable to play rumble! %s\n", SDL_GetError());
