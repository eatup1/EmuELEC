--- a/menu/menu_displaylist.c
+++ b/menu/menu_displaylist.c
@@ -11222,7 +11222,7 @@ bool menu_displaylist_ctl(enum menu_displaylist_ctl_state type,
             settings_t *settings         = config_get_ptr();
 #endif

-#ifdef HAVE_LAKKA
+#ifndef HAVE_LAKKA
             if (menu_entries_append_enum(info->list,
                      msg_hash_to_str(MENU_ENUM_LABEL_VALUE_UPDATE_LAKKA),
                      msg_hash_to_str(MENU_ENUM_LABEL_UPDATE_LAKKA),
--- a/menu/menu_setting.c
+++ b/menu/menu_setting.c
@@ -8363,7 +8363,7 @@ static bool setting_append_list(
 #endif
 #if !defined(IOS)
          /* Apple rejects iOS apps that let you forcibly quit them. */
-#ifdef HAVE_LAKKA
+#ifndef HAVE_LAKKA
          CONFIG_ACTION(
                list, list_info,
                MENU_ENUM_LABEL_QUIT_RETROARCH,

