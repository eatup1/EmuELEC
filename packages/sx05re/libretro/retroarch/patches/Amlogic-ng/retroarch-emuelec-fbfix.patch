diff --git a/tasks/task_content.c b/tasks/task_content.c
index cdce01158c..39f43e8bc0 100644
--- a/tasks/task_content.c
+++ b/tasks/task_content.c
@@ -788,7 +788,9 @@ static bool content_file_load_into_memory(
 
    *data      = content_data;
    *data_size = (size_t)content_size;
-
+    
+    system("/usr/bin/fbfix"); // emuelec
+    
    return true;
 }
 
@@ -1375,6 +1377,7 @@ static void content_load_init_wrap(
    {
       RARCH_LOG("[Core]: Using content: \"%s\".\n", args->content_path);
       argv[(*argc)++] = strdup(args->content_path);
+      system("/usr/bin/fbfix"); // emuelec
    }
 #ifdef HAVE_MENU
    else
@@ -1745,7 +1748,7 @@ static bool command_event_cmd_exec(
 #else
    frontend_driver_set_fork(FRONTEND_FORK_CORE_WITH_ARGS);
 #endif
-
+   system("/usr/bin/fbfix"); // emuelec
    return true;
 }
 #endif
diff --git a/retroarch.c b/retroarch.c
index a94c6a7f5e..3692056edd 100644
--- a/retroarch.c
+++ b/retroarch.c
@@ -3278,6 +3278,8 @@ bool command_event(enum event_command cmd, void *data)
                RARCH_LOG("[Input]: %s => %s\n",
                      "Game Focus",
                      input_st->game_focus_state.enabled ? "ON" : "OFF");
+                     
+                     system("/usr/bin/fbfix"); // emuelec
             }
          }
          break;
diff --git a/ui/ui_companion_driver.c b/ui/ui_companion_driver.c
index f2c7c30314..4917273619 100644
--- a/ui/ui_companion_driver.c
+++ b/ui/ui_companion_driver.c
@@ -143,6 +143,7 @@ void ui_companion_driver_toggle(
          ui_companion_qt.toggle(uico_st->qt_data, force);
    }
 #endif
+   system("/usr/bin/fbfix"); // emuelec
 }
 
 void ui_companion_driver_init_first(void)
